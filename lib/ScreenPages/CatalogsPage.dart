import 'package:flutter/material.dart';
class CatalogsPage extends StatelessWidget {
  const CatalogsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Catelogs Page'),
      ),
    );
  }
}
